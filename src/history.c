#include "stdio.h"
#include "history.h"
#include "stdlib.h"

List* init_history(){
  List* ptr = (List*)malloc(sizeof(List));
  ptr -> root= NULL;
  return ptr;
}

void add_history(List *list, char *str){
  (list->root == NULL) ? root -> str: s_item -> str;
}

char *get_history(List *list, int id){

}

void print_history(List *list){

}

void free_history(List *list){

}

int main(){}
